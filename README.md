# Ransomware Technical Demo

To run this, you will first need to install the *pycrypto* and *tkinter* Python libraries.
```console
user@host:~$ sudo apt-get install python3-tk
user@host:~$ pip install pycrypto
```

